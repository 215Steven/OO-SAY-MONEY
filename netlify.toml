[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  # 將前端呼叫的 /api/* 路徑自動轉給 Netlify Functions 處理
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  # React SPA 網址回退 (Fallback) 規則，確保重新整理時不會 404
  from = "/*"
  to = "/index.html"
  status = 200
